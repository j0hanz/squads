Outcome

Skills behave as a squad. Hooks do not, and dispatch-agents carries furniture from the shape it no longer serves. The dispatch-optimization the name promises is real and lives entirely in the skills: plan/SKILL.md:39-46 sizes waves per task (K = ceil(candidate_files/12), C = ceil(task_count/5), cap-and-announce), plan/SKILL.md:52 explicitly refuses to fan out under 50 files because "agent round-trips are slower", debug/SKILL.md:22-29 gates fan-out behind a single-thread-unless test, brainstorm/SKILL.md:113 forbids subagents in 5 of 6 phases. That is fast-completion-first with fan-out as means — hypothesis disconfirmed for skills/.

It is confirmed for hooks/. 1045 lines of hook (hooks.json 62, perf-hook.py 695, squads-hook.sh 288) enforce zero dispatch invariants. The one hook on the dispatch path greps for {{. 66% of hook mass exists to time the other 34%. Two stated invariants — no silent degrade, no silent cap — are violated by the hooks themselves. And dispatch-agents still ships a fleet-shape table describing dispatches that tdd and inline debug never make.

Findings

1. hooks/squads-hook.sh:87-107 + hooks/hooks.json:17 — the only hook on the dispatch path enforces nothing about dispatch. PreToolUse matches Agent|SendMessage|Workflow, reads 9 tool_input fields (:91), and checks one thing: unresolved {{...}}. Its own comment concedes scope: "dispatch-check is placeholder hygiene only — it does NOT enforce routing" (:97). Violates the principle because this is the exact interception point where fleet size, job size, and model tier are knowable, and none are inspected — every "smart, structured, task-oriented" claim rides on the model policing itself. Squad-shaped fix: at that seam, assert what is cheap and mechanical — model == 'haiku' per squads/SKILL.md:69, a fan-out counter per turn against squads/SKILL.md:39's ~10, a prompt-length/file-count ceiling for dispatch-agents/SKILL.md:131's granularity rule. Warn, don't deny.

2. hooks/perf-hook.py:104-110 — a HARD GATE silently fails open on timeout. On TimeoutExpired, every rule except dispatch-check returns err=b"", code=0. A debug-gate deny that takes >8s becomes an allowed edit with empty stderr; the only trace is "timeout":1 in a JSONL file nobody reads unless they run report. Directly violates squads/SKILL.md:39 "log anything truncated — silent caps read as full coverage" and the no-silent-degrade posture of :69. Fix: emit one stderr line on any timed-out gate ([WARN] squads <rule>: guard timed out — gate not applied), same as the model-unavailable WARN pattern the plugin already uses.

3. hooks/squads-hook.sh:127-130 — the debug HARD GATE auto-disarms after 120 minutes with no notice. find "$flag" -mmin +120 → rm -f "$flag"; return 0. Silent. A debug session crossing two hours loses reproduce-first enforcement and nothing says so; compact() at :257 honors the same expiry, so the recap won't mention it either. Same invariant violated as finding 2. Fix: print squads debug-gate: flagted, re-invokesquads:debug if still mid-debug to stderr before removing. 4. hooks/squads-hook.sh692 — a cosmetic lintcan block the entire fleet. dispatch_check denies every Agent/SendMessage/Workflow call when jq is absent, when the wrapper crand when the child timeee fail-closed paths,all guarding a {{ grep. On a machine without jq, the squad dispatches nothing — session_starth dies. Violatesfast-completion-of-the-right-work: the guard's failure mode is more costly than the thing it guards. Fix: fail open with a loud stderr WARN. Placeholder leakage problocked dispatchproduces no squad at all. 5. skills/dispatch-agenE fleet-shape list isdispatch-first residue, and two rows are wrong. :43 "tdd — One worker" describes a dispatch tdskill is main-threadRED-GREEN start to finish, zero Agent calls. :44 "debug — One investigator per hypothesis + fresh skeptics" describes composed-mode shape, but debug/SKILL.md:56 forbids Agent-dispatch fallback outright ("No fallback to turn-by-turn Agent dispatch"), so on preflight failure debug dispatches zero agents. Rows :27 ("Lifecycle match → inline") and :30 ("Trivial → inline") are unreachable — the rraight to its skill andone-shot edits to no skill, and :13 already says hand off anything that lands here. Name-vs-behavior: a table that claims to own fleet shapes it no longer sees, for skillsete the fleet-shape list and rows :27/:30; keep only what runs here — approved-plan execution and bulk fan-out. 6. hooks/perf-hook.py (:9,21,31,43,55 —observability outweighs the thing observed, on the default path. The Python wrapper is 2.4× the dispatcher it times, and hooks.json prefers it: python perf-hook.py <rule> first, bare bash only as fallback. Every Write/Edit/Agent/Skill call therefore pays Python startup + subprocess bash + jq to append one JSONL line. The bypass flag SQUADS_FAST (hooks.json:9, commit dec75ea "skip perf wrapper for lower hook latency") is self-indicting — the measurement layer needed a latency escape hatch, and it is off by default. Violates fast-completion: the plugin taxes every edit to measure its own ceremony. Fix: invert the default (bare dispatcher unless SQUADS_PERF=1), or cut that log and passthrough, and drop the 200-line report renderer (:194-287). 7. skills/dispatch-agenvernor's decision is acount against a constant. Mode selection reduces to "one-off ≥ cutoff (currently 5) → composed", and the fields that would make it task-aware are declared inert: "budget_tokens, agent_cap, success_criteria are guidance only, never runtime-enforced" (:76). So the Governor emits a 7-field struct (:52-62) of which one field (mode) is decided, by comparing an item count to5. That is mechanical, es the fleet to thetask" claim is carried by plan's scaling math, not by the Governor. Fix: either derive the cutoff from the actual signal (Files: overlap in the task graph, chunk count, reatruct to {mode, reason}and stop implying sizing that doesn't happen.
4. skills/dispatch-agents/SKILL.md:131 — "haiku-sized" has no mechanism on the path that needs it. The granularity rule is prose. The nearest enforcement, plan-schema (squads-hook.sh:147-156), checks that 7 field labels are present in a ### TASK-NNN: block — a task with Files: listing 40 paths and Action: rewrite the auth system passes the awk unchanged. The onlyreal size check is one d:135 Scope-Risk High"task touches >3 files", which runs only in contract/blueprint depth and only bites via the Step 9 thresholds (:145-148). Bulk/fan-out work entering dispatch-agents without all. Oversized jobsslip through exactly there. Fix: count Files: entries in the existing awkand deny above a statedady in the hook.

5. skills/brainstorm/scripts/scan_context.py:909-913 — silent caps feed the Context Report. interface_shapes[:10], constraints[:5], unknowns[:4], analogous_features[:2] are sliced with no total emitted; ScanResult (:39-48) has no truncation field and main dumps only the capped lists (:943)._MAX_CONSTRAINTS_PER_FIa file early, silently.Files are the partial exception — scope_reasoning carries the pre-cap total_matched (:765,884), so a reader can notice 23-matched-vs-5-listed. Everything else drops wrt thatbrainstorm/SKILL.md:23 uses to set Scope and :76 hands to critics as their only context. Violates squads/SKILL.md:39 verbatim. Fix: add a truncated: {constraints: "5/17", ...} field; it's four counters.
6. hooks/hooks.json:39 + hooks/squads-hook.sh:91 — the two seams best suited to invariant checking are unused. PostToolUse matches Skill|Write|Edit|MultiEdit|NotebookEdit — not Agent, so no hook ever sees a subagent return, and the Handoff Contract's "missing status or findings = FAIL" (squads/SKILL.md:43) is unenforced on every inline dispatch. dispatch_check's jq extraction (:91) reads prompt/message/script/description/summary/to/scriptPath/name/args — model is absent, so the flat-haiku policy (squads/SKILL.md:69) and its explicit "never a silent degrade" are honor-system. Compounding: dispatch-agents/SKILL.m.md:78 both note hooksdon't fire inside the native workflow runtime — so the hook layer is structurally absent precisely where fan-out is largest, and idle where it present. Fix: add Agentnd shape-check thereturn; add .tool_input.model to the dispatch_check extraction and WARN on absence.

Housekeeping (grouped, not padding): perf-hook.py:173 swallows all logging exceptions silently — the observability tool degrades unobserved; perf-hook.py:532 importhook.sh:173 skips bothedit-path gates when jq is missing (documented in README, silent in-session); perf-hook.py:658-667 costs 8s wall-clock in --self-check for one timeout assertion; itive on a legitimate {{ }} template literal inside a Workflow script payload; review/SKILL.md:39 fixes 2 reviewers regarle for a merge gate, but it is the one fan-out in the plugin that isn't sized to anything.
