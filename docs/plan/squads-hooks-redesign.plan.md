# squads hooks redesign — Plan

Status: DRAFT
Depth: blueprint
Origin: plan

REVISE pass: applies H1 (TASK-003 intra-task ordering) + M1-M10 + L1-L9 against the prior 9-task blueprint. Implements [specs](squads-hooks-redesign.specs.md) (REQ-001..REQ-013) from the [Design Brief](../design/2026-07-20-squads-hooks-redesign-design.md) (Approach C, user-locked). Sequencing: infrastructure → shadow build → atomic swap → ethos rules (wire-before-logic) → docs → final verify. 9 tasks, acyclic.

### TASK-001: Infrastructure — parse guard + line-ending policy

Depends on: none
Files: [package.json](../../package.json), [.gitattributes](../../.gitattributes)
Symbols: [scripts.format:check](../../package.json#L33)
Satisfies: REQ-010, REQ-008
Action: Extend `package.json` `scripts.format:check` to `bash -n hooks/*.sh && prettier --check .` (single script, no new one). Create `.gitattributes` with `*.sh text eol=lf` and `*.json text eol=lf`. Do NOT run `git add --renormalize` yet — defer to TASK-003 so renormalize lands with the consolidated file.
Validate: `cd /c/squads && npm run format:check && cat .gitattributes`
Expected result: `format:check` exits 0 (bash -n passes the 4 existing scripts, prettier passes); `.gitattributes` contains the two eol=lf rules.

### TASK-002: Build squads-hook.sh dispatcher in shadow + synthetic-JSON smoke

Depends on: TASK-001
Files: [hooks/squads-hook.sh](../../hooks/squads-hook.sh)
Symbols: hooks/squads-hook.sh (new file — shared lib + 4 rule fns + case dispatch + debug-gate Bash write-pattern heuristic)
Satisfies: REQ-001, REQ-002, REQ-008, REQ-009, REQ-013
Action: Write `hooks/squads-hook.sh` complete for the 4 existing rules, DORMANT (`hooks.json` still points at the old scripts). Top: shebang, header comment listing all `<rule>` subcommands and gate-lifts (tdd/plan/review), `set -uo pipefail` with one-line comment explaining the absent `-e`. Shared lib: `state_dir` (${TMPDIR:-/tmp} + `squads-<rule>-<sid>[-<key>]` namespace), `b64d` (base64 -d then -D), `jq_fail_closed` (jq required + actionable install hint covering all three platforms — Windows `winget install jqlang.jq`, macOS `brew install jq`, Linux `apt/dnf install jq` per REQ-009 — + exit 2 on missing), `atomic_write` (mktemp in same dir + mv), `deny` helper (names rule + one-line remediation). `case "$1"`dispatch, one fn per rule. Port`dispatch-check`with ALL defect fixes: read`.session_id`raw (drop`// "no-session-id"`default at [dispatch-check.sh#L56](../../hooks/dispatch-check.sh#L56)); add`.tool_input.description`to the inspected bodies ([dispatch-check.sh#L56](../../hooks/dispatch-check.sh#L56)); scan Workflow`.script`/`.scriptPath`bodies for the`squads:reviewer-dispatch`sentinel (not just the Task prompt — REQ-002, missing at [dispatch-check.sh#L138](../../hooks/dispatch-check.sh#L138) which only scans`$prompt`); widen raw-diff to `^--- `/`^+++ `/`^diff -` ([dispatch-check.sh#L107](../../hooks/dispatch-check.sh#L107)); `flock … || deny` ([dispatch-check.sh#L174](../../hooks/dispatch-check.sh#L174)) with `flock` best-effort comment corrected (absent on Git Bash + macOS — REQ-008); validate count-file integer ([dispatch-check.sh#L175](../../hooks/dispatch-check.sh#L175)); atomic mktemp+mv write ([dispatch-check.sh#L190](../../hooks/dispatch-check.sh#L190)); hash fallback fail-closed ([dispatch-check.sh#L147](../../hooks/dispatch-check.sh#L147)); hash full `Change summary:` block multiline ([dispatch-check.sh#L151](../../hooks/dispatch-check.sh#L151)). Port `debug-gate`: fail-close empty `tool_name` ([debug-gate.sh#L20](../../hooks/debug-gate.sh#L20)); add bare `test.*`/`spec.*`/`tests.*` to exemption glob ([debug-gate.sh#L57](../../hooks/debug-gate.sh#L57)); header comment lists tdd/plan/review as gate-lifts ([debug-gate.sh#L1](../../hooks/debug-gate.sh#L1)). REQ-013: add the debug-gate Bash write-pattern heuristic — while the debug-gate flag is set, inspect `tool_input.command` for write-to-file subcommands (conservative pattern list: `sed -i`, `tee`, `printf … >`, `cat … >`, `>`/`>>` redirects to a non-test/non-md path) and deny; route-to-sibling (tdd/plan/review) lifts the flag; 120min `find -mmin +120` expiry; pattern list marked `# ponytail: heuristic — pattern list is the calibration knob`. Port `session-start`: banner enumerates ALL hook events ([session-start.sh#L10](../../hooks/session-start.sh#L10)); `jq … 2>/dev/null || true`; dirname-based path ([session-start.sh#L9](../../hooks/session-start.sh#L9)). Port `session-end`: correct misleading empty-id comment ([session-end.sh#L15](../../hooks/session-end.sh#L15)); widen cleanup glob to `squads-*-${sid}*`([session-end.sh#L20](../../hooks/session-end.sh#L20)). Add stub arms`tdd-gate|tdd-arm|return-shape|plan-schema) echo "not yet wired" >&2; exit 0`for forward-compat (L3 — stderr notice, not silent exit 0, so manual invocation during the TASK-002→TASK-005 window is not a silent no-op). Note (L8): these stub arms are inert — no`hooks.json`route reaches them until TASK-004-007 wire them; REQ-012's no-orphan property is a FINAL-state guarantee verified mechanically by TASK-009, so the transient stub arms are not a violation. Do NOT touch`hooks.json`.
Validate: `cd /c/squads && bash -n hooks/squads-hook.sh && echo '{"tool_name":"Write","tool_input":{"file_path":"x.md"},"session_id":"t"}' | bash hooks/squads-hook.sh debug-gate; echo "debug-gate-exit=$?"; echo '{"tool_name":"Task","tool_input":{"prompt":"hello {{x}}"},"session_id":"t"}' | bash hooks/squads-hook.sh dispatch-check; echo "dispatch-check-exit=$?"; echo '{"session_id":"t","reason":"end"}' | bash hooks/squads-hook.sh session-end; echo "session-end-exit=$?"; echo '{"session_id":"t"}' | bash hooks/squads-hook.sh session-start; echo "session-start-exit=$?"; bash hooks/squads-hook.sh tdd-gate 2>&1 | grep -q "not yet wired"`Expected result:`bash -n`clean; debug-gate exits 0 (md exempt) and`debug-gate-exit=0`; dispatch-check exits 2 with "unresolved placeholder" diagnostic and `dispatch-check-exit=2`; session-end prints cleanup line and `session-end-exit=0`; session-start prints router block and `session-start-exit=0`; stub arm emits "not yet wired" on stderr. A broken implementation that returns the wrong code for any rule FAILS the Validate (M4/M5 — explicit exit-code assertions).

### TASK-003: Atomic swap — rewrite hooks.json exec-form + delete 4 old scripts

Depends on: TASK-002
Files: [hooks/hooks.json](../../hooks/hooks.json), hooks/dispatch-check.sh, hooks/debug-gate.sh, hooks/session-start.sh, hooks/session-end.sh
Symbols: [PreToolUse matcher](../../hooks/hooks.json#L18), [timeout](../../hooks/hooks.json#L23), [SessionStart command](../../hooks/hooks.json#L9), [SessionEnd command](../../hooks/hooks.json#L43)
Satisfies: REQ-001, REQ-007, REQ-008, REQ-013
Action: H1 — execute in this strict intra-task order: 1. rewrite `hooks/hooks.json` to exec form (every `command` becomes `bash "${CLAUDE_PLUGIN_ROOT}/hooks/squads-hook.sh" <rule>` for rules `session-start`, `dispatch-check`, `debug-gate`, `session-end`) — this is safe because `squads-hook.sh` already exists from TASK-002, so the rewrite points at a live file and there is no dangling-reference window; 2. `git rm` the 4 old scripts (`dispatch-check.sh`, `debug-gate.sh`, `session-start.sh`, `session-end.sh`); 3. `git add --renormalize` scoped to `*.sh *.json` (lands the TASK-001 `.gitattributes` rules). The rewrite-before-rm order is what guarantees no dangling-reference window. In the rewrite: drop the stale bare `Task` from the PreToolUse matcher ([hooks.json#L18](../../hooks/hooks.json#L18)), keep `Agent|SendMessage|Workflow`; raise all PreToolUse timeouts 10→30 ([hooks.json#L23](../../hooks/hooks.json#L23)); REQ-013 — expand the debug-gate PreToolUse matcher to `Skill|Write|Edit|MultiEdit|NotebookEdit|Bash` (add `Bash`); add a top-of-file `_comment` documenting the R2 fail-OPEN residual (command-hook timeout is non-blocking, unfixable, only mitigated; fail-closed survives).
Validate: `cd /c/squads && npm run format:check && git diff --cached --stat && git status --short hooks/`
Expected result: `format:check` (incl. `bash -n hooks/*.sh`) exits 0; `git diff --cached --stat` shows `hooks.json` modified, 4 old scripts deleted, `squads-hook.sh` added, `.gitattributes` + `package.json` from TASK-001; `hooks/` contains only `hooks.json` and `squads-hook.sh`.

### TASK-004: tdd-arm rule (PostToolUseFailure Bash) — wire before logic

Depends on: TASK-003
Files: [hooks/squads-hook.sh](../../hooks/squads-hook.sh), [hooks/hooks.json](../../hooks/hooks.json)
Symbols: hooks/squads-hook.sh (tdd_arm fn + case arm), hooks/hooks.json (PostToolUseFailure Bash entry)
Satisfies: REQ-004, REQ-012
Action: Add a `PostToolUseFailure` entry to `hooks/hooks.json` with matcher `Bash`, command `bash`, args `["${CLAUDE_PLUGIN_ROOT}/hooks/squads-hook.sh", "tdd-arm"]`, timeout 30. In the SAME task, add the `tdd_arm` fn and `tdd-arm)` case arm to `hooks/squads-hook.sh`: read `session_id` + `is_interrupt` from stdin JSON; arm (`touch "$TMPDIR/squads-tdd-red-<sid>"`) on any non-interrupt Bash failure, skip on interrupt or empty sid (sid sanitized via `tr -cd 'a-zA-Z0-9-'`); always exit 0 (passive hook, never blocks). jq missing → degrade silently (no gate to fail-close on). 120min `find -mmin +120` expiry backstop on the flag. Wire entry + case arm together — no half-wired state. (Design correction vs the original brief: `PostToolUse` fires only on Bash exit 0, so the non-zero case is `PostToolUseFailure`; the Bash `tool_response` has no `exit_code` field, only `stdout`/`stderr`/`interrupted`/`isImage`, and PostToolUseFailure carries `error` + `is_interrupt`.)
Validate: `cd /c/squads && export TMPDIR=/c/tmp/squads-smoke && rm -f "$TMPDIR"/squads-* && bash -n hooks/squads-hook.sh && echo '{"tool_name":"Bash","tool_input":{"command":"pytest"},"session_id":"t","is_interrupt":false,"error":"Command exited with non-zero status code 1"}' | bash hooks/squads-hook.sh tdd-arm; echo "arm-fail-exit=$?"; test -f "$TMPDIR/squads-tdd-red-t" && echo "flag-armed" && rm -f "$TMPDIR/squads-tdd-red-t"; echo '{"tool_name":"Bash","session_id":"t","is_interrupt":true}' | bash hooks/squads-hook.sh tdd-arm; test -f "$TMPDIR/squads-tdd-red-t" && echo "FAIL-armed-on-interrupt" || echo "interrupt-skipped"`
Expected result: `bash -n` clean; failure invocation exits 0 and creates `squads-tdd-red-t` (`flag-armed`); interrupt invocation exits 0 and does NOT create the flag (`interrupt-skipped`).

### TASK-005: tdd-gate rule (PreToolUse Write/Edit/MultiEdit/NotebookEdit) — wire before logic

Depends on: TASK-004
Files: [hooks/squads-hook.sh](../../hooks/squads-hook.sh), [hooks/hooks.json](../../hooks/hooks.json)
Symbols: hooks/squads-hook.sh (tdd_gate fn + case arm — references the ported debug-gate glob list in the debug-gate case arm of hooks/squads-hook.sh), hooks/hooks.json (PreToolUse entry)
Satisfies: REQ-003, REQ-012
Action: Add a `PreToolUse` entry to `hooks/hooks.json` with matcher `Write|Edit|MultiEdit|NotebookEdit`, command `bash "${CLAUDE_PLUGIN_ROOT}/hooks/squads-hook.sh" tdd-gate`, timeout 30. In the SAME task, add the `tdd_gate` fn and `tdd-gate)` case arm: deny non-test/non-md impl edits unless `squads-tdd-red-<sid>` flag exists; exempt glob = the ported debug-gate glob list in `hooks/squads-hook.sh` (the debug-gate case arm — L1, the old `[debug-gate.sh#L57]` reference pointed at a file deleted in TASK-003) PLUS bare `test.*`/`spec.*`/`tests.*`; 120min `find -mmin +120` expiry on the flag; deny message names the rule + one-line remediation ("run a failing test first; if RED is stale, remove $flag"). Wire entry + case arm together. L2 note: shares the `Write|Edit|MultiEdit|NotebookEdit` matcher with debug-gate; safe per R1 per-invocation isolation (each `hooks.json` entry is a separate process) — no Depends on TASK-002 needed.
Validate: `cd /c/squads && bash -n hooks/squads-hook.sh && touch "${TMPDIR:-/tmp}/squads-tdd-red-t" && echo '{"tool_name":"Write","tool_input":{"file_path":"src/impl.js"},"session_id":"t"}' | bash hooks/squads-hook.sh tdd-gate; echo "with-flag-exit=$?"; rm "${TMPDIR:-/tmp}/squads-tdd-red-t" && echo '{"tool_name":"Write","tool_input":{"file_path":"src/impl.js"},"session_id":"t"}' | bash hooks/squads-hook.sh tdd-gate; echo "no-flag-exit=$?"; echo '{"tool_name":"Write","tool_input":{"file_path":"tests/x.test.js"},"session_id":"t"}' | bash hooks/squads-hook.sh tdd-gate; echo "test-file-exit=$?"`Expected result:`bash -n` clean; with flag → exit 0 (`with-flag-exit=0`, impl edit permitted); without flag → exit 2 with tdd-gate deny (`no-flag-exit=2`); test file → exit 0 (`test-file-exit=0`, exempt) regardless of flag.

### TASK-006: return-shape rule (SubagentStop) — wire before logic

Depends on: TASK-003
Files: [hooks/squads-hook.sh](../../hooks/squads-hook.sh), [hooks/hooks.json](../../hooks/hooks.json)
Symbols: hooks/squads-hook.sh (return_shape fn + case arm + per-subagent state file), hooks/hooks.json (SubagentStop entry)
Satisfies: REQ-005, REQ-012
Action: Add a `SubagentStop` entry to `hooks/hooks.json` (no matcher), command `bash`, args `["${CLAUDE_PLUGIN_ROOT}/hooks/squads-hook.sh", "return-shape"]`, timeout 30. In the SAME task, add the `return_shape` fn and `return-shape)` case arm. Two shapes selected by content (design correction: the `squads:reviewer-dispatch` sentinel is in the dispatch prompt, NOT the SubagentStop payload, so a reviewer is detected by its mandated `## Code Review Result` response header): reviewer (`## Code Review Result` present) → require the 5 review headers (`## Code Review Result`, `**Status**: PASS|FAIL`, `### Blocking Issues`, `### Advisory Issues`, `### What Was Checked`); otherwise → regex `last_assistant_message` for `^status:\s*(PASS|FAIL|PARTIAL)` + `^findings:` (review format uses `**Status**:`, not `status:`, so the shapes are alternatives). 1st malformed → `exit 2` + stderr reminder (one retry); 2nd malformed → `exit 0` + stdout abort diagnostic. M2 — distinguish 1st from 2nd via a per-subagent state file `squads-return-shape-<sid>-<agent_id>` under `${TMPDIR:-/tmp}`: touch on 1st malformed, check-and-allow on 2nd, 120min `find -mmin +120` expiry; `<agent_id>` read from the SubagentStop payload, fall back to a hash of `last_assistant_message`. Wire entry + case arm together.
Validate: `cd /c/squads && bash -n hooks/squads-hook.sh && echo '{"last_assistant_message":"status: PASS\nfindings: ok","agent_id":"a1","session_id":"t"}' | bash hooks/squads-hook.sh return-shape; echo "ok-exit=$?"; echo '{"last_assistant_message":"no status line","agent_id":"a2","session_id":"t"}' | bash hooks/squads-hook.sh return-shape; echo "bad1-exit=$?"; echo '{"last_assistant_message":"still no status","agent_id":"a2","session_id":"t"}' | bash hooks/squads-hook.sh return-shape; echo "bad2-exit=$?"; echo '{"last_assistant_message":"squads:reviewer-dispatch\nh1\nh2\nh3\nh4","agent_id":"a3","session_id":"t"}' | bash hooks/squads-hook.sh return-shape; echo "reviewer-4hdr-exit=$?"; echo '{"last_assistant_message":"squads:reviewer-dispatch\nh1\nh2\nh3\nh4\nh5","agent_id":"a4","session_id":"t"}' | bash hooks/squads-hook.sh return-shape; echo "reviewer-5hdr-exit=$?"`
Expected result: `bash -n` clean; well-formed → `ok-exit=0`; 1st malformed (`a2`) → `bad1-exit=2` with stderr reminder and creates `squads-return-shape-t-a2`; 2nd malformed (`a2`, same agent_id) → `bad2-exit=0` with the abort diagnostic on stdout; reviewer sentinel with <5 headers → `reviewer-4hdr-exit=2`; reviewer sentinel with 5 headers → `reviewer-5hdr-exit=0` (M6).

### TASK-007: plan-schema rule (PreToolUse Write to docs/plan/*.plan.md) — wire before logic

Depends on: TASK-003
Files: [hooks/squads-hook.sh](../../hooks/squads-hook.sh), [hooks/hooks.json](../../hooks/hooks.json)
Symbols: hooks/squads-hook.sh (plan_schema fn + case arm + header comment documenting the Edit gap), hooks/hooks.json (PreToolUse entry)
Satisfies: REQ-006, REQ-012
Action: Add a `PreToolUse` entry to `hooks/hooks.json` with matcher `Write`, command `bash "${CLAUDE_PLUGIN_ROOT}/hooks/squads-hook.sh" plan-schema`, timeout 30. In the SAME task, add the `plan_schema` fn and `plan-schema)` case arm: read `tool_input.content`; if `Status: APPROVED` + `Depth: sketch` → deny; parse `### TASK-NNN:` blocks, require all 7 field labels (Depends on / Files / Symbols / Satisfies / Action / Validate / Expected result), deny naming the missing field; require `Origin:` header. L7 — the `plan-schema` header comment in `squads-hook.sh` documents the Edit gap (PreToolUse Edit can't see the full result, so this is a Write-only block). Wire entry + case arm together.
Validate: `cd /c/squads && bash -n hooks/squads-hook.sh && echo '{"tool_name":"Write","tool_input":{"file_path":"docs/plan/x.plan.md","content":"Status: APPROVED\nDepth: sketch\nOrigin: ideator\n### TASK-001: t\nDepends on: -\nFiles: a\nSymbols: a\nSatisfies: R\nAction: a\nValidate: a\nExpected result: a"},"session_id":"t"}' | bash hooks/squads-hook.sh plan-schema; echo "approved-sketch-exit=$?"; echo '{"tool_name":"Write","tool_input":{"file_path":"docs/plan/x.plan.md","content":"Status: DRAFT\nDepth: blueprint\nOrigin: plan\n### TASK-001: t\nDepends on: -\nFiles: a\nSymbols: a\nSatisfies: R\nAction: a\nValidate: a\nExpected result: a"},"session_id":"t"}' | bash hooks/squads-hook.sh plan-schema; echo "valid-exit=$?"; echo '{"tool_name":"Write","tool_input":{"file_path":"docs/plan/x.plan.md","content":"Status: DRAFT\nDepth: blueprint\nOrigin: plan\n### TASK-001: t\nDepends on: -\nFiles: a\nSymbols: a\nSatisfies: R\nAction: a\nExpected result: a"},"session_id":"t"}' | bash hooks/squads-hook.sh plan-schema; echo "missing-validate-exit=$?"; echo '{"tool_name":"Write","tool_input":{"file_path":"docs/plan/x.plan.md","content":"Status: DRAFT\nDepth: blueprint\n### TASK-001: t\nDepends on: -\nFiles: a\nSymbols: a\nSatisfies: R\nAction: a\nValidate: a\nExpected result: a"},"session_id":"t"}' | bash hooks/squads-hook.sh plan-schema; echo "missing-origin-exit=$?"`
Expected result: `bash -n` clean; APPROVED+sketch → `approved-sketch-exit=2` with deny; valid DRAFT+blueprint with all 7 fields + Origin → `valid-exit=0`; TASK block missing `Validate:` → `missing-validate-exit=2` with a deny naming the missing field; missing `Origin:` header → `missing-origin-exit=2` (M7).

### TASK-008: Docs pass — AGENTS.md + README.md

Depends on: TASK-003
Files: [AGENTS.md](../../AGENTS.md), [README.md](../../README.md)
Symbols: [AGENTS.md#L3](../../AGENTS.md#L3), [README.md#L28](../../README.md#L28), [README.md#L32](../../README.md#L32)
Satisfies: REQ-011
Action: Fix [AGENTS.md#L3](../../AGENTS.md#L3) "the repo is skills plus one Node hook" → bash-dispatcher reality (no Node, jq required; one `hooks/squads-hook.sh <rule>` dispatcher). Update [README.md#L28](../../README.md#L28): single `squads-hook.sh <rule>` dispatcher + exec-form `hooks.json` + 30s timeout + R2 fail-OPEN residual note. Update [README.md#L32](../../README.md#L32) `hooks/session-start.sh` reference → router block now lives in the dispatcher. Remove any "Node hook" / "Node runtime" claim from both files.
Validate: `cd /c/squads && ! grep -nE 'Node|node hook' AGENTS.md README.md; echo "no-node-exit=$?"; grep -q 'squads-hook.sh' AGENTS.md && grep -q 'squads-hook.sh' README.md; echo "found-dispatcher-exit=$?"`
Expected result: `no-node-exit=0` (inverted grep — no stale "Node"/"node hook" references remain; a stale reference FAILS the Validate with non-zero, M8); `found-dispatcher-exit=0` (`squads-hook.sh` referenced in both files).

### TASK-009: Final verification — no orphan rules, full smoke, /hooks check

Depends on: TASK-003, TASK-004, TASK-005, TASK-006, TASK-007, TASK-008
Files: none
Symbols: none
Satisfies: REQ-012
Action: Run the full verification sweep: `bash -n` on `hooks/squads-hook.sh`; confirm every `case` arm has exactly one `hooks.json` entry and vice versa (no orphans — M3/M9, mechanical diff, not prose); one pass + one fail synthetic-JSON smoke per rule (4 existing + 4 ethos); confirm `session-end` cleans `squads-*-<sid>*` state; confirm `git diff --cached --stat` is clean and complete; user runs `/hooks` in a fresh session to confirm all 8 entries loaded.
Validate: `cd /c/squads && bash -n hooks/squads-hook.sh && npm run format:check && arms=$(grep -oE '^\s+[a-z-]+\)\s' hooks/squads-hook.sh | tr -d ' )' | sort); entries=$(grep -oE 'squads-hook.sh [a-z-]+' hooks/hooks.json | awk '{print $2}' | sort); diff <(printf '%s\n' "$arms") <(printf '%s\n' "$entries"); echo "orphan-diff-exit=$?"; echo '{"session_id":"final-verify","reason":"end"}' | bash hooks/squads-hook.sh session-end; ! ls "${TMPDIR:-/tmp}/squads-"*final-verify* 2>/dev/null; echo "no-leftover-exit=$?"`
Expected result: `bash -n` clean; `format:check` exits 0; `orphan-diff-exit=0` (mechanical diff of case arms vs hooks.json commands is empty — final 8↔8 mapping, no orphans, M3/M9); `no-leftover-exit=0` (inverted `ls` — no `squads-*-final-verify*` state survives session-end, M3/M9); `/hooks` (user-run) shows all 8 entries.

---

## Synthesis rationale

REVISE pass — applied H1 (TASK-003 intra-task ordering: rewrite→rm→renormalize, explicit) + M1 (TASK-002 re-added Workflow .script/.scriptPath sentinel scan) + M2 (TASK-006 return-shape per-subagent state file) + M3/M9 (TASK-009 mechanical orphan diff + inverted leftover-state check) + M4/M5 (TASK-002 explicit exit-code assertions in Validate) + M6 (TASK-006 2nd-malformed + reviewer-sentinel smokes) + M7 (TASK-007 missing-field + missing-Origin smokes) + M8 (TASK-008 inverted grep) + M10 (Parallelization note below) + L1 (TASK-005 glob-list reference repointed at squads-hook.sh) + L2 (TASK-005 matcher-sharing note) + L3 (TASK-002 stub arms emit stderr notice) + L5 (TASK-002 flock best-effort comment) + L6 (TASK-002 jq install hint all three platforms) + L7 (TASK-007 plan-schema header comment documents Edit gap) + L8 (TASK-002 stub arms inert, REQ-012 final-state) + L9 (TASK-002 + TASK-003 REQ-013 debug-gate +Bash matcher and write-pattern heuristic).

Kept Minimalist's coarse seam "do not split the single-file port" — the 4 existing-rule ports all live in one file; splitting into per-rule tasks (Conventional) produces intermediate states where `squads-hook.sh` exists with fns missing, strictly worse than one complete write. TASK-002 is kept as one task (M4/M5: strengthened Validate with explicit exit-code assertions instead of splitting — a broken implementation now FAILS the Validate). Discarded Minimalist's 3-task collapse because the 400-line dispatcher write needs a real synthetic-JSON smoke Validate, not just `bash -n`. Kept Risk-First's shadow-build safety: build the dispatcher while old scripts still carry traffic, validate without touching `hooks.json`, then swap `hooks.json` to exec-form + delete the 4 old scripts in ONE atomic task (H1: rewrite-before-rm order guarantees no dangling-reference window). Kept Risk-First's wire-before-logic guardrail: each ethos rule is its own task wiring the `hooks.json` entry and case arm together — no hook file ever half-wired (user's hard constraint: no orphan hook files; REQ-012 is a FINAL-state property, verified mechanically by TASK-009's diff — transient stub arms in TASK-002 are inert, no hooks.json route reaches them). Kept Risk-First's infrastructure-first ordering (parse guard + `.gitattributes` before any runtime change — zero production risk). Discarded Conventional's separate docs tasks (merged into one docs pass — none touch runtime). Ordering: infrastructure → shadow build → atomic swap → ethos rules (tdd-arm before tdd-gate so the gate's flag exists) → docs → final verify; acyclic.

Parallelization note (M10): only `005→004` (tdd-arm arms the flag before tdd-gate consumes it) and `009→all` (final verify needs every prior task) are true ordering constraints. Once TASK-003 lands, TASK-006 (return-shape), TASK-007 (plan-schema), and TASK-008 (docs) fan out in parallel with each other AND with the TASK-004→TASK-005 chain — a linear reader must not serialize these four parallelizable tasks. TASK-004 must precede TASK-005; TASK-009 must follow all.
